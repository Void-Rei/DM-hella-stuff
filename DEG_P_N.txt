//Маслов К.Д. 9309 

unsigned long long DEG_P_N(DM_P *P) // Степень многочлена
{
	unsigned long long d = (*P).m;

	for (long long i = (*P).m; i > 1; i--)	//Если первые коэффициенты являются 0 то уменьшаем степень

		if (*P).c[i] != 0 then

			return d;

		else

			d--;

	return d;
}