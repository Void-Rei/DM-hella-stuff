//Маслов К.Д. 9309 

unsigned short DEG_P_N(struct DM_P& M) // Степень многочлена
{
	unsigned short d = M.m;

	for (int i = M.m; i > 1; i--)	//Если первые коэффициенты являются 0 то уменьшаем степень
	{
		if (M.n != 0)
		{
			return d;
		}
		else d--;
	}

	return d;
}